<script>
  // Navbar scroll effect
  window.addEventListener("scroll", function () {
    const navbar = document.querySelector(".custom-navbar");
    if (navbar) {
      navbar.classList.toggle("scrolled", window.scrollY > 50);
    }
  });

  // Scroll animations for fade-in and fade-up elements
  const fadeElements = document.querySelectorAll('.fade-in, .fade-up');

  const scrollOptions = {
    threshold: 0.2,
    rootMargin: "0px 0px -50px 0px"
  };

  const scrollObserver = new IntersectionObserver((entries, observer) => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add('appear');
      observer.unobserve(entry.target);
    });
  }, scrollOptions);

  fadeElements.forEach(el => scrollObserver.observe(el));
</script>
<!-- AOS JS -->
<script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
<script>
  AOS.init({
    duration: 800, // animation duration
    offset: 200,    // distance from the bottom to trigger
    once: true      // animate only once
  });
</script>


<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<footer class="bg-dark text-white text-center py-3 mt-5">
  <p>&copy; 2025 Deseret SACCO. All rights reserved.</p>
</footer>